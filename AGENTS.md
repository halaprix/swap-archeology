# Swap Archeology

Read README.md and docs/ADAPTERS.md before substantial work. BRIEF.md, SOURCE_DISCOVERY.md and ROUTING_ALGORITHM.md hold the current scope and proposed architecture.

- Historical, read-only liquidity/routing research. No live transactions, production changes, commits or pushes unless the user later explicitly changes that scope.
- Preserve evidence/ as copied reference material. Keep new outputs separate and retain chain/block hashes, raw responses and source identities. Never copy or print RPC credentials.
- Use all supported sources in one routing universe; show unresolved and unavailable sources explicitly. Do not assume unlimited pegs, instant queued withdrawals or independent copies of shared liquidity.
- Keep discovery, snapshots, source semantics, solver and evaluation modular. Use existing protocol math/tools when correct. Validate nontrivial arithmetic and shared-state behavior with independent checks.
- If .codegraph exists, use CodeGraph before code-structure searches. If .beads exists, use bd-explore before raw issue searches and verify bd where before mutations. Do not point this project at another repository's issue store.
- Preserve the local-only knowledge boundary. No Hindsight/wiki submission or other external knowledge writes without explicit user authorization.
